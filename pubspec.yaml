name: offline_first
description: A test project for research
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  path_provider: ^2.1.0
  sembast: ^3.5.0+1
  sembast_sqflite: ^2.2.0
  sembast_web: ^2.1.3
  sqflite: ^2.3.0

flutter:
  uses-material-design: true
